# AI-Neural_Style_Transfer
It is a AI project that is consists with the Encoder &amp; Decoder model which is the part of the Transformer architechture model ,and it is the model that has  two inputs one is content image and one is style image then after the certain time we get the output of Stylized Result of the images 
